import { AuthorScopeDirective } from './author-scope.directive';

describe('AuthorScopeDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorScopeDirective();
    expect(directive).toBeTruthy();
  });
});
