import { PasteUrlDirective } from './paste-url.directive';

describe('PasteUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new PasteUrlDirective();
    expect(directive).toBeTruthy();
  });
});
